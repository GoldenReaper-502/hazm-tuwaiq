# Hazm Tuwaiq

**HSE & Risk Management Platform with AI-Powered Detection**

منصة متكاملة لإدارة المخاطر والسلامة مع كشف ذكي مدعوم بـ AI

---

## ✨ الميزات

- **📷 كاميرا مباشرة**: معاينة فيديو حية مع معالجة تلقائية للمشاكل
- **🤖 كشف ذكي**: كشف الأجسام والمخاطر باستخدام AI في الوقت الفعلي
- **💬 دردشة تفاعلية**: أسئلة وأجوبة مع السياق من نتائج الكشف
- **📊 إدارة البلاغات**: إنشاء وتتبع الحوادث والمخاطر
- **📁 رفع الملفات**: تخزين الصور والتقارير
- **🔐 أمان**: دعم API Keys اختياري، CORS محسّن
- **📱 واجهة RTL**: دعم كامل للعربية

---

## 🚀 البدء السريع

### المتطلبات

- Python 3.9+
- Node.js 14+ (اختياري - للتطوير)
- متصفح حديث يدعم WebRTC (Chrome, Firefox, Edge, Safari)

### التثبيت والتشغيل

#### 1. Backend (FastAPI)

```bash
cd backend

# نسخ الإعدادات
cp ../.env.example .env
# عدّل .env حسب احتياجاتك

# تثبيت المكتبات
pip install -r requirements.txt

# تشغيل الخادم
uvicorn app:app --reload --host 0.0.0.0 --port 8000

# سيكون API متاحاً على: http://localhost:8000
# Docs (Swagger): http://localhost:8000/docs
```

#### 2. Frontend (تطوير محلي)

**الخيار 1: استخدام Python للتطوير المحلي**

```bash
cd frontend

# استخدام Python's built-in server (Python 3)
python -m http.server 3000

# أو للـ Python 2 (قديم):
# python -m SimpleHTTPServer 3000

# افتح: http://localhost:3000
```

**الخيار 2: استخدام أي web server آخر**

```bash
# npx (إذا كان Node.js مثبتاً)
npx http-server -p 3000

# أو استخدم أي web server آخر (nginx, Apache, etc)
```

---

## 📖 أدلة الاستخدام

### ⚙️ تكوين API

عند فتح الموقع للمرة الأولى:

1. أدخل **API URL**: `http://localhost:8000` (للتطوير المحلي)
2. اترك **API KEY** فارغ (إذا كنت بدون حماية)
3. اضغط **حفظ**

```
ملاحظة: تُحفظ الإعدادات في localStorage تلقائياً
```

### 📷 استخدام الكاميرا

#### الخطوات الأساسية:

1. **اختر جهاز الكاميرا**: من قائمة "اختر جهاز الكاميرا"
2. **اضغط "ابدأ الكاميرا"**
3. **وافق على صلاحية الكاميرا** في نافذة المتصفح
4. **فعّل الكشف التلقائي** (اختياري)

#### مؤشرات الحالة:

- 🟢 **تعمل**: الكاميرا تعمل بشكل صحيح
- 🟡 **تحضير**: جاري تحضير الكاميرا
- 🔴 **خطأ**: توقف الكاميرا أو حدث خطأ
- 🟠 **محاولة جديدة**: إعادة محاولة الاتصال

#### استكشاف الأخطاء:

| الخطأ | السبب | الحل |
|------|------|------|
| **شاشة سوداء** | الكاميرا لا ترسل بيانات | أعد تشغيل المتصفح، أغلق التطبيقات الأخرى التي تستخدم الكاميرا |
| **صلاحيات مرفوضة** | رفضت الصلاحيات | تحقق من إعدادات خصوصية المتصفح |
| **HTTPS مطلوب** | سياق غير آمن | استخدم `localhost` أو `https` |
| **لا توجد كاميرا** | لم يتم العثور على جهاز | تأكد من توصيل الكاميرا |
| **NotReadableError** | الكاميرا مستخدمة | أغلق التطبيقات الأخرى (Zoom, Teams, etc) |

**صفحة اختبار الكاميرا المتقدمة:**
```
http://localhost:3000/camera-test.html
```

استخدمها لفحص:
- دعم المتصفح
- الصلاحيات
- أجهزة الكاميرا المتاحة
- معلومات الإطار

### 🤖 الكشف الذكي

1. **فعّل الكشف**: ضع علامة ✓ على "تفعيل الكشف التلقائي"
2. **انتظر النتائج**: يتم الكشف كل 1.2 ثانية تلقائياً
3. **اعرض النتائج**: تظهر النتائج في قسم "نتائج الكشف الأخيرة"

#### ميزات الكشف:

- ✓ عرض الأجسام المكتشفة مع درجة الثقة
- ✓ overlay على الفيديو (مربعات خضراء)
- ✓ آخر وقت تحديث
- ✓ معالجة الأخطاء تلقائية

### 💬 الدردشة والأسئلة

1. **اكتب سؤالك** في حقل الإدخال
2. **اضغط "إرسال"** أو اضغط `Enter`
3. **اقرأ الرد** من النظام

#### ميزات خاصة:

- **"اسأل عن هذا الكشف"**: ربط سؤالك بنتائج الكشف الأخيرة تلقائياً
- **سجل الدردشة**: يُحفظ تلقائياً في localStorage
- **مسح الدردشة**: اضغط "مسح الدردشة" لحذف السجل

### 📝 البلاغات والحوادث

1. **ملأ النموذج**:
   - العنوان: مثال "انزلاق في الممر"
   - الموقع: مثال "Gate 3"
   - الخطورة: منخفضة / متوسطة / عالية / حرجة
   - الوصف: تفاصيل الحادثة

2. **إرسال البلاغ**: اضغط "إرسال"

3. **عرض البلاغات**: اضغط "تحديث" في قسم "قائمة البلاغات"

---

## 🧪 الاختبار والتحقق

### اختبار Backend

```bash
cd backend
python validate.py
```

**ماذا يختبر:**
- ✓ `/health` - حالة الخادم
- ✓ `/detect` - كشف الأجسام
- ✓ `/chat` - الدردشة
- ✓ `/chat/{session_id}` - سجل الدردشة

### اختبار Camera

**الطريقة 1: صفحة الويب**
```
http://localhost:3000/camera-test.html
```

**الطريقة 2: اختبار برمجي**
```javascript
// في DevTools console
navigator.mediaDevices.enumerateDevices()
  .then(devices => console.log(devices))
```

---

## 📊 تجربة سريعة (3 دقائق)

### 1. تشغيل الخادم (30 ثانية)

```bash
cd backend
pip install -r requirements.txt
uvicorn app:app --reload
```

### 2. تشغيل الموقع (30 ثانية)

```bash
cd frontend
python -m http.server 3000
# افتح: http://localhost:3000
```

### 3. اختبار الميزات (2 دقيقة)

**الكاميرا:**
- [ ] اختر كاميرا
- [ ] اضغط "ابدأ الكاميرا"
- [ ] وافق على الصلاحيات
- [ ] تأكد من ظهور الفيديو

**الكشف:**
- [ ] فعّل "الكشف التلقائي"
- [ ] انتظر نتائج الكشف
- [ ] شاهد الأشكال على الفيديو

**الدردشة:**
- [ ] اكتب: "مرحباً"
- [ ] اضغط "إرسال"
- [ ] اقرأ الرد
- [ ] اضغط "اسأل عن هذا الكشف"

**البلاغات:**
- [ ] املأ نموذج الحادثة
- [ ] اضغط "إرسال"
- [ ] اضغط "تحديث" لعرض البلاغ

---

## 🔧 البنية المعمارية

```
hazm-tuwaiq/
├── backend/
│   ├── app.py              # FastAPI الرئيسي (incidents, chat, detect)
│   ├── main.py             # بديل (غير مستخدم حالياً)
│   ├── requirements.txt     # المكتبات المطلوبة
│   ├── validate.py         # اختبار التحقق من الـ endpoints
│   └── runtime.txt         # إصدار Python (للـ Render)
│
├── frontend/
│   ├── index.html          # الصفحة الرئيسية
│   ├── app.js              # JavaScript الرئيسي (Camera, Chat, Detection)
│   ├── styles.css          # أنماط التصميم
│   ├── camera-test.html    # صفحة اختبار الكاميرا
│   └── assets/             # الصور والملفات
│
├── .env.example            # قالب متغيرات البيئة
├── .gitignore              # الملفات المستبعدة من Git
└── README.md              # هذا الملف
```

---

## 🔐 الأمان

### متغيرات البيئة

```bash
# .env
HAZM_API_KEY=your-secret-key-here
CORS_ORIGINS=http://localhost:3000,https://yourdomain.com
LOG_LEVEL=INFO
```

### API Key

إذا أردت حماية الـ API:

```bash
# Backend - ضع مفتاح API في متغير البيئة
export HAZM_API_KEY="your-key"

# Frontend - أدخل نفس المفتاح في حقل "API KEY"
```

### CORS

تم تفعيل CORS لـ local development. في الإنتاج:

```python
# backend/app.py
allow_origins=["https://yourdomain.com"]
```

---

## 📝 ملاحظات التطوير

### تعطيل الـ Logs

في `frontend/app.js`:

```javascript
const IS_DEV = false;  // تعطيل الـ console logs
```

### إضافة LLM حقيقي

في `backend/app.py`, عدّل دالة `generate_chat_response`:

```python
# استخدم OpenAI, Anthropic Claude, etc.
response = openai.ChatCompletion.create(
    model="gpt-3.5-turbo",
    messages=[...],
)
```

### إضافة نموذج AI للكشف

في `backend/app.py`, عدّل endpoint `/detect`:

```python
# بدل YOLOv8 mock بـ نموذج حقيقي
detection = yolov8_model.predict(image)
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: الكاميرا لا تفتح

**التشخيص:**
1. افتح `http://localhost:3000/camera-test.html`
2. انقر "فحص الصلاحيات"
3. انقر "اكتشف الأجهزة"

**الحل:**
- [ ] أغلق التطبيقات الأخرى التي تستخدم الكاميرا
- [ ] أعد تشغيل المتصفح
- [ ] تأكد من `https` أو `localhost`

### المشكلة: API غير متصل

```bash
# تحقق من أن Backend يعمل
curl http://localhost:8000/health

# يجب أن ترى:
# {"status":"ok","service":"...","time_utc":"...","version":"0.1.0"}
```

### المشكلة: الدردشة لا تستجيب

```bash
# اختبر endpoint الدردشة
python backend/validate.py

# أو
curl -X POST http://localhost:8000/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"hello"}'
```

---

## 📚 المراجع

- [WebRTC API](https://developer.mozilla.org/en-US/docs/Web/API/WebRTC_API)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [Canvas API](https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API)
- [Fetch API](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API)

---

## 📄 الترخيص

MIT License

---

## 👥 المساهمة

نرحب بالمساهمات! يرجى فتح Issue أو Pull Request.

---

**آخر تحديث:** ديسمبر 2025

بالحزم نواجه المخاطر... وبطويق نصمد 🛡️

## 🚀 Quick Start (Development)

```bash
# Install dependencies
cd backend
pip install -r requirements.txt
pip install ultralytics reportlab openpyxl opencv-python-headless pillow numpy  # Optional AI/Export

# Set environment
cp .env.example .env

# Run backend (localhost:8000)
uvicorn app:app --reload

# Test in another terminal
curl http://localhost:8000/health
```

## 📦 Deployment

See [DEPLOYMENT_CHECKLIST.md](DEPLOYMENT_CHECKLIST.md) for step-by-step Render/Railway deployment.

**Quick summary:**
1. Push to GitHub
2. Create Render/Railway web service
3. Set environment variables
4. Deploy! (~5 min build time)

## 📚 API Documentation

- **Swagger UI**: `/docs`
- **ReDoc**: `/redoc`
- **OpenAPI Schema**: `/openapi.json`

## 🔧 Key Features

- ✅ **YOLOv8 Detection** - Real-time object detection (mock fallback available)
- ✅ **Smart Reports** - Auto-generate PDF/Excel reports from detections
- ✅ **LLM Chat** - OpenAI/Anthropic integration with detection context
- ✅ **Export** - JSON, CSV, PDF, Excel export formats
- ✅ **Production Ready** - Docker, HTTPS, CORS, logging, health checks

## 🎯 Next Steps

1. **Frontend**: Build React/Vue dashboard and connect to backend
2. **Database**: Add PostgreSQL for persistent storage
3. **Monitoring**: Set up Sentry for error tracking
4. **CI/CD**: GitHub Actions for automated testing
5. **Scaling**: Load testing and optimization

See individual issue templates for detailed task specifications.
