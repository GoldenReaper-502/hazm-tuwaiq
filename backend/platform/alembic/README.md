# Alembic Migrations (Stub)

This folder is prepared for SQL migrations in production.

Suggested initialization commands:
1. `alembic init backend/platform/alembic`
2. Configure `sqlalchemy.url` to SQLite/Postgres.
3. Generate revision from models and apply.
