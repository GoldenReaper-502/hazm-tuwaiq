# Hazm Tuwaiq Environment Variables
# Copy this to .env and fill in your values

# ============== Server Configuration ==============
PORT=8000
LOG_LEVEL=INFO
PYTHONUNBUFFERED=1

# ============== CORS Configuration ==============
# Comma-separated list of allowed origins
# Default allows localhost (dev), set to specific domains in production
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8000

# ============== Security ==============
# Optional API key for endpoint protection
# If not set, API is publicly accessible
HAZM_API_KEY=

# ============== YOLOv8 Detection Configuration ==============
# Model to use: yolov8n.pt (nano, 280MB), yolov8s.pt (small, 1GB), etc.
YOLO_MODEL=yolov8n.pt

# Confidence threshold for detections (0.0-1.0)
# Lower = more detections, higher = fewer false positives
YOLO_CONF=0.25

# Device to run inference on: cpu (slow), cuda (fast, requires NVIDIA GPU)
TORCH_DEVICE=cpu

# ============== Smart Reports Configuration ==============
# Risk score threshold for auto-generating reports (0.0-1.0)
# Reports are auto-created only when risk_score >= this threshold
REPORT_RISK_THRESHOLD=0.3

# Site/location name for generated reports
SITE_LOCATION=Default Site

# ============== LLM Integration (Optional) ==============
# Choose one of:
# - OpenAI: Set OPENAI_API_KEY
# - Anthropic: Set ANTHROPIC_API_KEY
# - Neither: Falls back to rule-based responses

# OpenAI GPT-4 or GPT-3.5-turbo
OPENAI_API_KEY=

# Anthropic Claude
ANTHROPIC_API_KEY=

# ============== Frontend Configuration ==============
# Frontend will be served on a separate port (e.g., 3000)
VITE_API_URL=http://localhost:8000

# ============== Database (if using PostgreSQL in future) ==============
# DATABASE_URL=postgresql://user:password@localhost:5432/hazm_db

# ============== Deployment Platforms ==============
# For Render.com:
#   - Set PORT=8000
#   - Set CORS_ORIGINS to your frontend domain
#   - Use PostgreSQL add-on (optional)
#
# For Railway.app:
#   - Same as Render
#   - Railway detects Procfile automatically
#
# For Vercel (frontend):
#   - Set VITE_API_URL to your backend domain (e.g., https://hazm-api.render.com)
