# 🔧 دليل حل المشاكل

## خطأ: "Unexpected token '<', "<!doctype "... is not valid JSON"

### السبب
هذا الخطأ يحدث عندما يتوقع التطبيق استجابة JSON من Backend لكنه يحصل على HTML بدلاً منها.

### الحلول

#### ✅ 1. تأكد من تشغيل Backend

```bash
# في terminal منفصل
cd /workspaces/hazm-tuwaiq
source .venv/bin/activate
python backend/app.py
```

يجب أن ترى:
```
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000
```

#### ✅ 2. اختبر Backend يدوياً

افتح متصفح جديد واذهب إلى:
```
http://localhost:8000
```

يجب أن ترى JSON مثل:
```json
{
  "name": "HAZM API",
  "version": "1.0.0",
  "status": "running"
}
```

إذا رأيت HTML أو صفحة خطأ، فهذا يعني Backend لا يعمل بشكل صحيح.

#### ✅ 3. تحقق من رسائل الخطأ في Frontend

الآن Frontend يعطيك رسائل أوضح:
- افتح Developer Console (F12)
- انظر إلى رسائل الخطأ
- ستخبرك بالضبط ما المشكلة

#### ✅ 4. إعدادات Frontend

في التطبيق:
1. اذهب إلى تبويب **⚙️ الإعدادات**
2. تأكد من API URL صحيح:
   - للتطوير المحلي: `http://localhost:8000`
   - للإنتاج: URL الخاص بـ Render
3. اضغط **حفظ**
4. اذهب إلى تبويب **🏥 الحالة**
5. اضغط **اختبار الاتصال**

#### ✅ 5. مشاكل CORS

إذا كان Backend يعمل لكن Frontend لا يستطيع الاتصال:

**للتطوير المحلي:**
```bash
# تأكد من أن Backend يسمح بـ CORS من localhost
# في backend/app.py، تحقق من:
# CORS_ORIGINS="*" أو "http://localhost:5500,http://127.0.0.1:5500"
```

**للإنتاج على Render:**
في Render Dashboard → Environment Variables:
```
CORS_ORIGINS=https://hazm-frontend.onrender.com,https://hazm-backend.onrender.com
```

#### ✅ 6. إعادة تشغيل كل شيء

```bash
# أوقف كل شيء (Ctrl+C)

# أعد تشغيل Backend
cd /workspaces/hazm-tuwaiq
source .venv/bin/activate
python backend/app.py

# في متصفح جديد
# افتح Frontend وجرب مرة أخرى
```

## مشاكل أخرى شائعة

### البرنامج بطيء جداً
- النموذج يحمل في أول مرة (قد يأخذ 1-2 دقيقة)
- بعدها سيكون سريع

### الكاميرا لا تعمل
1. السماح للمتصفح بالوصول للكاميرا
2. تحديث صفحة المتصفح
3. جرب كاميرا أخرى من القائمة

### API Key لا يعمل
1. تأكد من نفس الـ Key في Backend و Frontend
2. احفظ في الإعدادات
3. أعد تحميل الصفحة

## الحصول على مساعدة

إذا استمرت المشكلة:

1. **افتح Developer Console** (F12 → Console)
2. **انسخ رسالة الخطأ الكاملة**
3. **تحقق من Backend logs** (في Terminal حيث يعمل Backend)
4. **شارك المعلومات** للحصول على مساعدة

---

✅ **تم تحسين معالجة الأخطاء في النسخة الحالية**
الآن Frontend يعطيك رسائل أوضح عن المشاكل!
